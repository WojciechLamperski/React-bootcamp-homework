import { People } from '../data/People';
import { StyledContent } from './styles/Content.styled';

export const Content = () =>{
    return(
        <StyledContent>
            < People />
        </StyledContent>
    );
}